import React from 'react'
import Divider from 'material-ui/Divider'

const style = {
  marginTop: '10px',
  marginBottom: '4px',
  color: '#eee'
}

const Separator = () => <Divider style={style} />

export default Separator
